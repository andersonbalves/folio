# Set this to the absolute path of the monorepo root so Dynaconf finds settings.yaml
# Tip: add `export ROOT_PATH_FOR_DYNACONF=$(git rev-parse --show-toplevel)` to your shell profile
ROOT_PATH_FOR_DYNACONF=/path/to/folio
